{
  "name": "@iocalc/conformance",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "description": "Conformance helpers for IOCALC-compatible agent environment implementations.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm check"
  },
  "dependencies": {
    "@iocalc/protocol": "workspace:*"
  }
}

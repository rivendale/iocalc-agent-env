{
  "name": "@iocalc/example-http-player",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "conformance": "tsx src/conformance.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm check"
  },
  "dependencies": {
    "@iocalc/adapters": "workspace:*",
    "@iocalc/conformance": "workspace:*"
  }
}
